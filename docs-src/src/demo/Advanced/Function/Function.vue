<template>
    <Doc>
        <Function :data="`
        /**
         * 自定义校验函数
         * @param {object}  rule            当前规则对象
         * @param {*}       value           验证的值
         * @param {any}     callback        回调对象，如果验证成功，执行callback()；如果验证失败，执行callback('错误提示')
         * @return {*} 返回值
         */
        `"></Function>
    </Doc>
</template>